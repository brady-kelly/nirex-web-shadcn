model Category {
  id        Int@id  @default(autoincrement())
  name      String
  desc      String?
}

model ProductAttribute {
  id    Int@id  @default(autoincrement())
  name  String
  value String?
}

model Product {
  id      Int@id  @default(autoincrement())
  name    String
  variant String?
  desc    String?
}